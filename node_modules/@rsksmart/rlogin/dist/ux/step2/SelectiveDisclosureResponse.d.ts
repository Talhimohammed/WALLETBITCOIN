import { Data, DataField, SD } from '../../lib/sdr';
interface SelectiveDisclosureProps {
    data: Data;
    backendUrl: string;
    onConfirm: (sd: SD) => void;
}
interface DataListProps {
    dataField: DataField;
    areCredentials: boolean;
    select: (key: string, value: string) => void;
}
declare const DataList: ({ dataField, areCredentials, select }: DataListProps) => JSX.Element;
declare const SelectiveDisclosureResponse: ({ data: { credentials, claims }, backendUrl, onConfirm }: SelectiveDisclosureProps) => JSX.Element;
export { DataList, SelectiveDisclosureResponse };
//# sourceMappingURL=SelectiveDisclosureResponse.d.ts.map