import { Web3Provider } from '../web3provider/types';
import { EncryptionManagerConfig, GetEncryptionPublicKeyFn, IEncryptionManager } from './types';
declare class EncryptionManager implements IEncryptionManager {
    getEncryptionPublicKey: GetEncryptionPublicKeyFn;
    private decryptInWallet;
    constructor({ getEncryptionPublicKey, decrypt }: EncryptionManagerConfig);
    decrypt: (data: string) => Promise<string>;
    encrypt: (data: string) => Promise<string>;
    static fromWeb3Provider(provider: Web3Provider): Promise<EncryptionManager>;
}
export default EncryptionManager;
//# sourceMappingURL=asymmetric.d.ts.map